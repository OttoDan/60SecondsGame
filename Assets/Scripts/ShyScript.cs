using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ShyScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	//void Update () {
 //       if(Vector3.Distance(transform.position,PlayerController.Instance.transform.position) < 4)
 //       {
 //           transform.forward = Vector3.Lerp(transform.forward, Quaternion.AngleAxis(90, transform.up) * Vector3.Cross(-(PlayerController.Instance.transform.position - transform.position).normalized, transform.up),Time.deltaTime);

 //       }
	//}
}
